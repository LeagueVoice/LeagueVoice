{
  "rules": {
    "apiproxy": {
      ".read": "auth != null && auth.token.isAdmin == true",
      ".write": "auth != null && auth.token.isAdmin == true"
    },
    "users": {
      ".read": "true",
      ".write": "true"
      //".read": "auth != null",
      //".write": "auth != null"
    }
  }
}
